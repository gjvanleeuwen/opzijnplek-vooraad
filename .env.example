# Lightspeed R-Series (Retail POS) — client credentials only, tokens are stored in DB via OAuth
LS_RETAIL_CLIENT_ID=
LS_RETAIL_CLIENT_SECRET=

# Lightspeed eCom C-Series (webshopapp)
LS_ECOM_API_KEY=
LS_ECOM_API_SECRET=
LS_ECOM_LANGUAGE=nl

# Database
DATABASE_PATH=./data/opzijnplek.db

ORIGIN=""

# Better Auth (optional — for dashboard password protection)
BETTER_AUTH_SECRET=""
